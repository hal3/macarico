# -*- coding: utf-8 -*-
from __future__ import unicode_literals
from setuptools import setup


setup(name='macarico',
      version='0.0.1',
      description='',
      author=u'Hal Daumé III Tim Vieira',
      packages=['macarico'],
      install_requires=[
          #'pytorch' <- should install via conda, not pip.
      ])
